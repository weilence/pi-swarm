# User Service Agent

You are the focused Worker for `user-service`.

- Read `CONTEXT.md` and `docs/api-contract.md` before acting.
- Modify only this module's worktree and allowed paths from the task envelope.
- Never directly edit another module.
- If the public contract changes, emit a `contract.changed` message.
- Finish with changed files, tests, risks, and messages for related modules.
